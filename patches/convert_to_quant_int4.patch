diff --git a/convert_to_quant/cli/argument_parser.py b/convert_to_quant/cli/argument_parser.py
index 89ee1a2..3048c98 100644
--- a/convert_to_quant/cli/argument_parser.py
+++ b/convert_to_quant/cli/argument_parser.py
@@ -15,6 +15,7 @@ from ..constants import MODEL_FILTERS
 
 EXPERIMENTAL_ARGS = {
     "int8",
+    "int4",
     "nvfp4",
     "mxfp8",
     "make_hybrid_mxfp8",
@@ -247,7 +248,7 @@ class MultiHelpArgumentParser(argparse.ArgumentParser):
         print("-" * 40)
 
         format_args = [
-            "int8", "nvfp4", "mxfp8", "convrot", "convrot_group_size", "dynamic_convrot", "make_hybrid_mxfp8", "tensor_scales_path", "fallback",
+            "int8", "int4", "nvfp4", "mxfp8", "convrot", "convrot_group_size", "dynamic_convrot", "make_hybrid_mxfp8", "tensor_scales_path", "fallback",
             "block_size", "scaling_mode"
         ]
         for action in self._all_actions:
diff --git a/convert_to_quant/cli/main.py b/convert_to_quant/cli/main.py
index 0374f52..9655391 100644
--- a/convert_to_quant/cli/main.py
+++ b/convert_to_quant/cli/main.py
@@ -132,6 +132,12 @@ def get_parser() -> MultiHelpArgumentParser:
         "--comfy_quant", "--comfy-quant", action="store_true", dest="comfy_quant", help="Use Comfy quantization method."
     )
     parser.add_argument("--int8", action="store_true", help="Use INT8 block-wise quantization instead of FP8.")
+    parser.add_argument(
+        "--int4", action="store_true",
+        help="Use INT4 W4A4 ConvRot quantization (group-wise Hadamard rotation + row-wise INT4). Implies --convrot. "
+             "Every layer's in_features must be divisible by the ConvRot group size (default 256), or conversion "
+             "will raise an error; combine with --dynamic-convrot to auto-resolve a compatible group size per layer."
+    )
     parser.add_argument(
         "--convrot", action="store_true",
         help="Enable group-wise Hadamard rotation (ConvRot) for INT8 row-wise quantization to improve quality."
@@ -982,8 +988,8 @@ def run_conversion(args):
         print("Note: --comfy_quant auto-enabled (required for --custom-type mixed precision)")
         args.comfy_quant = True
 
-    # Only check FP8 support if not using INT8
-    if not args.int8:
+    # Only check FP8 support if not using INT8 or INT4
+    if not args.int8 and not args.int4:
         try:
             _ = torch.zeros(1, dtype=TARGET_FP8_DTYPE, device="cuda" if torch.cuda.is_available() else "cpu")
         except (RuntimeError, TypeError):
@@ -995,7 +1001,10 @@ def run_conversion(args):
         # Build filename: {simple_|learned_}{format}[mixed]_{scaling}
         # TODO: SVD stats (k, top_p, lr) should be saved to _convert_to_quant_stats metadata entry
         prefix = "simple_" if args.simple else "learned_"
-        if args.int8:
+        if args.int4:
+            format_str = "int4"
+            scaling_str = "_convrot_w4a4"
+        elif args.int8:
             format_str = "int8"
             if args.scaling_mode == "tensor":
                 scaling_str = "_tensorwise"
@@ -1035,7 +1044,9 @@ def run_conversion(args):
     # Call convert_to_fp8_scaled with explicit args (no **kwargs footgun)
     # Determine primary_format for NVFP4/MXFP8 mixed mode (when they fall through here)
     primary_format = None
-    if args.nvfp4 and (args.custom_type or args.fallback):
+    if args.int4:
+        primary_format = "int4"
+    elif args.nvfp4 and (args.custom_type or args.fallback):
         primary_format = "nvfp4"
     elif args.mxfp8 and (args.custom_type or args.fallback):
         primary_format = "mxfp8"
@@ -1067,7 +1078,7 @@ def run_conversion(args):
         custom_full_precision_mm=args.custom_full_precision_mm,
         custom_convrot=args.custom_convrot,
         custom_convrot_group_size=args.custom_convrot_group_size,
-        convrot=args.convrot,
+        convrot=args.convrot or args.int4,
         convrot_group_size=args.convrot_group_size,
         dynamic_convrot=args.dynamic_convrot,
         # Fallback options
diff --git a/convert_to_quant/constants.py b/convert_to_quant/constants.py
index d3da423..0045d88 100644
--- a/convert_to_quant/constants.py
+++ b/convert_to_quant/constants.py
@@ -298,6 +298,11 @@ INT8_MIN = int(torch.iinfo(TARGET_INT8_DTYPE).min)  # -128
 INT8_MAX = int(torch.iinfo(TARGET_INT8_DTYPE).max)  # 127
 INT8_SYMMETRIC_MAX = min(abs(INT8_MIN), INT8_MAX)  # 127 (symmetric range)
 
+# INT4 ConvRot W4A4 constants
+TARGET_INT4_DTYPE = torch.int8  # Packed storage dtype (2 int4 values per byte)
+INT4_SYMMETRIC_MAX = 7  # Signed int4 symmetric range [-7, 7]
+INT4_QUANT_GROUP_SIZE = 64  # Fixed int4 MMA tiling granularity (hardware constraint)
+
 # FP4 NVFP4 E2M1 constants
 TARGET_FP4_DTYPE = torch.uint8  # Packed format (2 values per byte)
 FP4_E2M1_MAX = 6.0
@@ -351,6 +356,7 @@ VALID_QUANT_FORMATS = {
     "float8_e4m3fn_block3d",
     "int8_blockwise",
     "int8_tensorwise",
+    "int4_convrot_w4a4",  # INT4 W4A4 ConvRot quantization
     "nvfp4",  # NVIDIA FP4 E2M1 block quantization
     "mxfp8",  # Microscaling FP8 block quantization
     "hybrid_mxfp8",  # Hybrid MXFP8 (MXFP8 + tensorwise fallback)
diff --git a/convert_to_quant/converters/learned_rounding.py b/convert_to_quant/converters/learned_rounding.py
index 0c56e8a..7b0067e 100644
--- a/convert_to_quant/converters/learned_rounding.py
+++ b/convert_to_quant/converters/learned_rounding.py
@@ -90,6 +90,11 @@ class LearnedRoundingConverter(BaseLearnedConverter):
         self.scale_optimization = scale_optimization
         self.has_bias = True
 
+        # INT4 W4A4 ConvRot is always row-wise and always rotated (no unrotated INT4 mode)
+        if target_format == "int4":
+            scaling_mode = "row"
+            self.convrot = True
+
         # INT8 defaults to block-wise scaling, but allows tensor-wise and row-wise
         if target_format == "int8" and scaling_mode not in ("tensor", "row", "block"):
             scaling_mode = "block"
@@ -99,13 +104,13 @@ class LearnedRoundingConverter(BaseLearnedConverter):
         self.scaling_mode = scaling_mode
 
         # Check ConvRot validity
-        if self.convrot and not (self.target_format == "int8" and self.scaling_mode == "row"):
-            verbose("  - WARNING: ConvRot is currently only supported for INT8 row-wise quantization. It will be ignored.")
+        if self.convrot and self.target_format not in ("int4",) and not (self.target_format == "int8" and self.scaling_mode == "row"):
+            verbose("  - WARNING: ConvRot is currently only supported for INT8 row-wise and INT4 quantization. It will be ignored.")
             self.convrot = False
             self.dynamic_convrot = False
 
         # Set format-specific max values and dtype
-        if self.target_format == "int8":
+        if self.target_format in ("int8", "int4"):
             self.target_dtype = TARGET_INT8_DTYPE
             self.f8_max_val = None
         else:
@@ -687,6 +692,15 @@ class LearnedRoundingConverter(BaseLearnedConverter):
                     # Determine if we should optimize
                     if torch.all(W_float32 == 0):
                         verbose("  - Tensor is all zeros, skipping optimization.")
+
+                        if self.target_format == "int4":
+                            out_features, in_features = W_float32.shape
+                            quantized_tensor = torch.zeros(
+                                out_features, in_features // 2, dtype=self.target_dtype, device=self.device
+                            )
+                            dequant_scale = torch.ones(out_features, device=self.device, dtype=SCALE_DTYPE)
+                            return quantized_tensor, dequant_scale, torch.zeros_like(W_float32), {}
+
                         quantized_tensor = torch.zeros_like(W_float32, dtype=self.target_dtype)
                         dequant_scale = None
 
@@ -720,8 +734,12 @@ class LearnedRoundingConverter(BaseLearnedConverter):
 
                         return quantized_tensor, dequant_scale, torch.zeros_like(W_float32), {}
 
-                    # INT8 quantization path
-                    if self.target_format == "int8":
+                    # INT4 W4A4 ConvRot quantization path
+                    if self.target_format == "int4":
+                        qdata, scale, dequantized = self._convert_int4_convrot_w4a4(
+                            W_float32, calibration_data=calibration_data
+                        )
+                    elif self.target_format == "int8":
                         if self.scaling_mode in ("tensor", "row"):
                             qdata, scale, dequantized = self._convert_int8_tensorwise(
                                 W_float32, calibration_data=calibration_data
@@ -968,6 +986,74 @@ class LearnedRoundingConverter(BaseLearnedConverter):
 
     # _convert_int8_rowwise merged into _convert_int8_tensorwise
 
+    def _convert_int4_convrot_w4a4(
+        self, W_float32: torch.Tensor, calibration_data: Optional[torch.Tensor] = None
+    ) -> Tuple[torch.Tensor, torch.Tensor, torch.Tensor]:
+        """
+        INT4 W4A4 ConvRot quantization: mandatory group-wise Hadamard rotation,
+        row-wise symmetric INT4 quantization (range [-7, 7]), optional AdaRound.
+
+        Return-space note: unlike `_convert_int8_tensorwise`'s ConvRot path (which
+        returns `dequantized` in the ROTATED basis, so `X_rot @ dequant.T == X @ W.T`),
+        this method un-rotates before returning, so its `dequantized` output is in
+        the ORIGINAL, un-rotated basis (`X @ dequant.T == X @ W.T`). Callers comparing
+        against a rotated activation (e.g. `X_rot`) must account for this difference.
+        """
+        from ..utils.convrot import (
+            build_hadamard,
+            pack_int4_row_major,
+            quantize_int4_rowwise,
+            rotate_weight,
+        )
+        from ..utils.tensor_utils import prepare_calibration_data
+        from ..constants import INT4_SYMMETRIC_MAX
+
+        _, N = W_float32.shape
+        layer_group_size = self.convrot_group_size
+        if self.dynamic_convrot:
+            from ..utils.convrot import find_max_compatible_group_size
+            layer_group_size = find_max_compatible_group_size(N, min_group_size=self.convrot_group_size)
+
+        if layer_group_size is None or N % layer_group_size != 0:
+            raise ValueError(
+                f"INT4 ConvRot W4A4 requires in_features ({N}) divisible by convrot_group_size ({layer_group_size})."
+            )
+
+        H = build_hadamard(layer_group_size, device=self.device, dtype=COMPUTE_DTYPE)
+        W_rot = rotate_weight(W_float32, H, layer_group_size)
+        verbose(f"    - Applied ConvRot Hadamard rotation (group_size={layer_group_size}).")
+
+        X_rot, Y_ref, _ = prepare_calibration_data(
+            W_rot, calibration_data, True, layer_group_size, self.device, COMPUTE_DTYPE, calib_scale=self.calib_scale
+        )
+
+        qdata, scale = quantize_int4_rowwise(W_rot)
+
+        if not self.no_learned_rounding and self.num_iter > 0:
+            verbose("    - Applying learned rounding optimization for INT4 ConvRot W4A4...")
+            qdata, scale = self._optimize_int8_adaround(W_rot, qdata, scale, X_rot, Y_ref, qmax=INT4_SYMMETRIC_MAX)
+
+        scale_broadcast = scale.unsqueeze(1)
+        dequantized_rotated = qdata.to(COMPUTE_DTYPE) * scale_broadcast
+        # H is symmetric and orthogonal (an involution): applying rotate_weight again undoes the rotation.
+        dequantized = rotate_weight(dequantized_rotated, H, layer_group_size)
+
+        # Phase 4: Residual Bias Calibration (mirrors the INT8 ConvRot path)
+        if self.has_bias and X_rot is not None and Y_ref is not None:
+            with torch.no_grad():
+                Y_quant = X_rot @ dequantized_rotated.T
+                bias_adj = (Y_ref - Y_quant).mean(dim=0)
+                self._current_extra_tensors["bias_correction"] = bias_adj.cpu()
+                verbose(
+                    f"    - Phase 4: Residual Bias Calibration (bias correction norm: {bias_adj.norm().item():.6f})"
+                )
+
+        packed = pack_int4_row_major(qdata)
+
+        self._cleanup_tensors(W_rot, X_rot, Y_ref)
+
+        return packed, scale.to(device=self.device, dtype=SCALE_DTYPE), dequantized
+
     def _optimize_int8_tensorwise_learned_rounding(self, W_float32: torch.Tensor, qdata: torch.Tensor,
                                                    scale: torch.Tensor) -> Tuple[torch.Tensor, torch.Tensor]:
         """
@@ -1061,11 +1147,15 @@ class LearnedRoundingConverter(BaseLearnedConverter):
         return dequantized
 
     def _optimize_int8_adaround(
-        self, W_float32: torch.Tensor, qdata: torch.Tensor, scale: torch.Tensor, X_rot: torch.Tensor, Y_ref: torch.Tensor
+        self, W_float32: torch.Tensor, qdata: torch.Tensor, scale: torch.Tensor, X_rot: torch.Tensor, Y_ref: torch.Tensor,
+        qmax: int = INT8_SYMMETRIC_MAX
     ) -> Tuple[torch.Tensor, torch.Tensor]:
         """
         Apply SVD-guided AdaRound (learned soft rounding) optimization over the rotated parameter space
         minimizing local activation reconstruction MSE using the cached calibration data.
+
+        Args:
+            qmax: Symmetric clamp range for the discretized output, e.g. 127 for INT8 or 7 for INT4.
         """
         M, N = W_float32.shape
 
@@ -1312,8 +1402,7 @@ class LearnedRoundingConverter(BaseLearnedConverter):
         with torch.no_grad():
             final_h_V = torch.sigmoid(best_V)
             final_round_up = (final_h_V >= 0.5).to(COMPUTE_DTYPE)
-            final_qdata = (W_floor + final_round_up).clamp(-INT8_SYMMETRIC_MAX,
-                                                           INT8_SYMMETRIC_MAX).round().to(TARGET_INT8_DTYPE)
+            final_qdata = (W_floor + final_round_up).clamp(-qmax, qmax).round().to(TARGET_INT8_DTYPE)
 
             verbose(f"    - Discretization audit: {best_converged_ratio * 100:.2f}% of parameters converged to strict boundaries.")
 
diff --git a/convert_to_quant/formats/fp8_conversion.py b/convert_to_quant/formats/fp8_conversion.py
index 33710d1..fc9ac63 100644
--- a/convert_to_quant/formats/fp8_conversion.py
+++ b/convert_to_quant/formats/fp8_conversion.py
@@ -179,7 +179,7 @@ def convert_to_fp8_scaled(
 
     # Get format-aware block_size default (converters handle their own fixed sizes)
     # This is only used for metadata/display; converters use their __init__ defaults
-    format_block_sizes = {"nvfp4": 16, "mxfp8": 32, "int8": 128, "fp8": 64}
+    format_block_sizes = {"nvfp4": 16, "mxfp8": 32, "int8": 128, "int4": 64, "fp8": 64}
     block_size = converter_kwargs.get("block_size") or format_block_sizes.get(target_format, 64)
 
     # Helper function to create converter for a specific format type
@@ -372,6 +372,8 @@ def convert_to_fp8_scaled(
                     layer_format = "fp8"
                 elif fmt.startswith("int8"):
                     layer_format = "int8"
+                elif fmt.startswith("int4"):
+                    layer_format = "int4"
                 else:
                     layer_format = "fp8"  # fallback
 
@@ -472,6 +474,7 @@ def convert_to_fp8_scaled(
 
         # Determine format type for this layer
         is_int8 = layer_format == "int8"
+        is_int4 = layer_format == "int4"
         is_mxfp8 = layer_format == "mxfp8"
         is_nvfp4 = layer_format == "nvfp4"
 
@@ -606,6 +609,15 @@ def convert_to_fp8_scaled(
                 # Add input_scale only for block-wise INT8 (dynamic quantization for rowwise doesn't use it)
                 if comfy_quant_format == "int8_blockwise":
                     new_tensors[f"{base_name}.input_scale"] = torch.tensor(1.0, dtype=torch.float32, device="cpu")
+            elif is_int4:
+                new_tensors[f"{base_name}.weight_scale"] = dequant_s.to(device="cpu", dtype=SCALE_DTYPE).detach().clone()
+                comfy_quant_format = "convrot_w4a4"
+                block_size_for_meta = None
+                comfy_quant_tensor = create_comfy_quant_tensor(
+                    "convrot_w4a4",
+                    full_precision_matrix_mult=layer_full_precision_mm if layer_full_precision_mm else None,
+                    convrot=True, convrot_groupsize=convrot_group_size,
+                )
             else:
                 # FP8 format - determine format based on scaling_mode or layer_config
                 new_tensors[f"{base_name}.weight_scale"] = dequant_s.to(device="cpu", dtype=SCALE_DTYPE).detach().clone()
diff --git a/convert_to_quant/utils/convrot.py b/convert_to_quant/utils/convrot.py
index 9244572..37f99f3 100644
--- a/convert_to_quant/utils/convrot.py
+++ b/convert_to_quant/utils/convrot.py
@@ -140,3 +140,83 @@ def find_max_compatible_group_size(in_features: int, min_group_size: int = 256)
             return group_size
         group_size //= 4
     return None
+
+
+def quantize_int4_rowwise(x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
+    """Row-wise symmetric INT4 quantization: scale = absmax / 7, range [-7, 7].
+
+    Matches comfy-kitchen's ``quantize_signed_int4_rowwise`` formula exactly
+    so packed output round-trips against comfy-kitchen's kernels.
+
+    Args:
+        x: Shape (rows, cols), any float dtype.
+
+    Returns:
+        Tuple of (qdata, scale):
+        - qdata: torch.int8, same shape as x, values in [-7, 7] (unpacked, one byte per value).
+        - scale: torch.float32, shape (rows,).
+    """
+    absmax = x.abs().amax(dim=-1).clamp(min=1e-10)
+    scale = absmax / 7.0
+    qdata = (x / scale.unsqueeze(-1)).round().clamp(-7, 7).to(torch.int8)
+    return qdata, scale.to(torch.float32)
+
+
+def pack_int4_row_major(qdata: torch.Tensor) -> torch.Tensor:
+    """Pack signed int4 values (stored as int8 in [-7, 7]) 2-per-byte, row-major.
+
+    Even column index -> low nibble, odd column index -> high nibble.
+
+    Args:
+        qdata: torch.int8, shape (rows, cols), cols must be even.
+
+    Returns:
+        torch.int8, shape (rows, cols // 2).
+    """
+    if qdata.shape[-1] % 2 != 0:
+        raise ValueError(f"pack_int4_row_major requires an even number of columns, got {qdata.shape[-1]}")
+    low = qdata[..., 0::2].to(torch.uint8) & 0x0F
+    high = qdata[..., 1::2].to(torch.uint8) & 0x0F
+    packed = (low | (high << 4)).to(torch.int8)
+    return packed
+
+
+def unpack_int4_row_major(qdata_packed: torch.Tensor) -> torch.Tensor:
+    """Inverse of pack_int4_row_major.
+
+    Args:
+        qdata_packed: torch.int8, shape (rows, packed_cols).
+
+    Returns:
+        torch.int8, shape (rows, packed_cols * 2), values in [-7, 7].
+    """
+    packed_u8 = qdata_packed.to(torch.uint8)
+    low = packed_u8 & 0x0F
+    high = (packed_u8 >> 4) & 0x0F
+
+    def _to_signed_nibble(v: torch.Tensor) -> torch.Tensor:
+        # 4-bit two's complement: values 8-15 represent -8..-1
+        return torch.where(v >= 8, v.to(torch.int16) - 16, v.to(torch.int16)).to(torch.int8)
+
+    low_signed = _to_signed_nibble(low)
+    high_signed = _to_signed_nibble(high)
+
+    rows, packed_cols = qdata_packed.shape
+    out = torch.empty(rows, packed_cols * 2, dtype=torch.int8, device=qdata_packed.device)
+    out[..., 0::2] = low_signed
+    out[..., 1::2] = high_signed
+    return out
+
+
+def dequantize_int4_rowwise(qdata_packed: torch.Tensor, scale: torch.Tensor) -> torch.Tensor:
+    """Unpack and dequantize row-wise INT4 data.
+
+    Args:
+        qdata_packed: torch.int8, shape (rows, packed_cols).
+        scale: torch.float32, shape (rows,).
+
+    Returns:
+        torch.float32, shape (rows, packed_cols * 2).
+    """
+    qdata = unpack_int4_row_major(qdata_packed)
+    return qdata.to(torch.float32) * scale.to(torch.float32).unsqueeze(-1)
